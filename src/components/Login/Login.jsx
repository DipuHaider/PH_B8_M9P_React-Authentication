import { Helmet } from "react-helmet-async";

const Login = () => {
    return (
        <div>
            <Helmet>
                <title>RA | Login</title>
            </Helmet>
            <h2>Login</h2>
        </div>
    );
};

export default Login;